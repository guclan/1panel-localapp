additionalProperties:
  formFields:
    - default: 389
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Port
      labelZh: 端口
      required: true
      rule: paramPort
      type: number
    - default: 
      edit: true
      envKey: LDAP_ORGANISATION
      labelEn: LDAP organisation
      labelZh: LDAP 组织名称
      required: true
      rule: paramCommon
      type: text
    - default:
      edit: true
      envKey: LDAP_DOMAIN
      labelEn: LDAP domain
      labelZh: LDAP 域名
      required: true
      rule: paramCommon
      type: text
    - default:
      edit: true
      envKey: LDAP_ADMIN_PASSWORD
      labelEn: LDAP admin password
      labelZh: LDAP 管理员密码
      required: true
      rule: paramComplexity
      type: password